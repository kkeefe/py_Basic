#include <stdio.h>

int main(int argc, char** argv)
{
    printf("hi there fren..\n");
    int boob(0);
    while(true)
    {
        boob++;
        if(boob >= 5)
            break;
    }
    boob = -1;
    printf("we've made it fren!\n");
    return 0; 
 }